import styled from 'styled-components';

import { SearchCard } from '../styled';

// DEsign will be on top of the searchcard div
export const StyledActorCard = styled(SearchCard)`
  .deathday {
    margin: 0;
    margin-top: 15px;
    font-weight: bold;
  }
`;
